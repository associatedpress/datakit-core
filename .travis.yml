language: python
python: 3.5

env:
  - TOXENV=py35
  - TOXENV=py34
  - TOXENV=py33

install: pip install -U tox
script: tox -e ${TOXENV}

deploy:
  provider: pypi
  distributions: sdist bdist_wheel
  user: zstumgoren
  password:
    secure: PLEASE_REPLACE_ME
  on:
    tags: true
    repo: associatedpress/datakit-core
    condition: $TOXENV == py35
