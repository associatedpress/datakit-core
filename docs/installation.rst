.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install datakit-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv datakit-core
    $ pip install datakit-core
